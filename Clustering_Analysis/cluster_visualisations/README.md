# Cluster visualisations

Generated by `../clustering_analysis.ipynb`:

- `elbow_method.png` — inertia curve with the elbow marked, plus silhouette scores
- `cluster_visualisation.png` — the segments in 2D, each centroid labelled
- `cluster_profiles.png` — what distinguishes each segment

Commit these once the notebook has been run against the real dataset — the
rubric asks for the clusters to be visualised *and labelled*.
